# Core ownership

Pure deterministic selection and validation live here. No database, HTTP, provider, or UI imports. The current Composer is only editorial-unkept-v1. Do not describe it as the complete recommender. Add cognitive utility, diversity, provenance, uncertainty, and exposure-aware evaluation through the full Composer issue and target design. Changing ranking requires policy versions and journey evidence.
