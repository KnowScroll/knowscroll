# Worker ownership

Own short deterministic projections now; provider execution later. Current project_keep runs within one transaction and makes no external calls. SKIP LOCKED distributes work, not exactly-once external side effects. AI SDK adapters, attempts, reservations, leases/fences, cancellation and stale proposal gates require the reasoning runtime issue before paid calls. Provider keys only here. Cutroom host paths are not mobile URLs; imported assets need KnowScroll provenance and publication gates.
