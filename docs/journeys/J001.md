# J001 — Keep a sourced encounter

| Steering question | Answer |
|---|---|
| Desired outcome | A person opens a Scroll, keeps it and can return to the retained path. The next discovery changes for an explainable reason. |
| Selected architecture | Native Android → HTTP admission → PostgreSQL Ledger/job → separate worker → Accounts/Trace → Composer. |
| Expected runtime | Persist a selection; report exposure only when visible; keep references exposure; one event/job despite retry; worker projects atomically; universe shows Trace; next decision excludes kept asset. |
| Observed runtime | Real HTTP/worker/database path passed locally on 2026-09-15. Android keep/return and Activity recreation passed. The isolated repeat also passed unavailable-server and compact-screen recovery checks. |
| Evidence | `artifacts/j001-runtime.json` contains event, causation, decision, exposure and job IDs. Durable reviewed evidence is linked below. |
| Divergence | Full world emergence, real identity, Reel generation and useful semantic ranking are outside this slice. An explicit kept Trace is not a semantic planet or learning inference. |
| Remaining decisions | Rich Scroll renderer/continuity; authentication; full Composer/semantic jobs. Track #2–#9. |

## Implementation proof

`pnpm typecheck`; `pnpm test` creates and drops a fresh test database, verifies admission/duplicate conflicts, ownership at the dev boundary, selection vs exposure, atomic rollback and retry. Android build and lint passed locally and in [CI](https://github.com/KnowScroll/knowscroll/actions/runs/35007382446). There are no Android unit-test sources in this bootstrap; mobile behavior is verified by the three real instrumentation runs below.

## Runtime/product proof

Run API and worker separately, then `pnpm verify:journey`. It consumes one unkept editorial asset and preserves history. It fails honestly on library exhaustion. Do not reset personal state to get a green receipt; use an isolated environment when repeating from a new universe.

On an Android emulator/device: open app → enter Scroll → inspect source and reason → Keep → return → observe retained Trace → reopen discovery → verify the kept asset is excluded. Confirm the same IDs in the database receipt. Also stop the API and verify visible retry without fabricated content/success; restore it and recover.

A source-backed paragraph and a working Keep do not prove improved mental models or the finished interaction grammar. Owner feedback and later longitudinal evaluation are separate evidence.

## Reviewed bootstrap evidence

- [Android causal receipt](evidence/bootstrap/j001-android.json), [source hashes and environment](evidence/bootstrap/environment.json).
- [Real HTTP development receipt](evidence/bootstrap/j001-http-development.json); [development Android receipt](evidence/bootstrap/j001-android-development.json).
- [Scroll](evidence/bootstrap/j001-scroll.png), [return](evidence/bootstrap/j001-return.png), [compact layout](evidence/bootstrap/j001-compact.png), [unavailable](evidence/bootstrap/j001-unavailable.png), [recovered](evidence/bootstrap/j001-recovered.png).
- Three instrumentation runs passed; raw concise results are beside these artifacts. API36 arm64 emulator; 1080×2400 and 840×1680 render sizes. Images were visually inspected.

The isolated journey app has a `.journey` application suffix and API port4311. Its test database was dropped after evidence collection; the normal development database and app were preserved. The source-hash manifest ties the observation to the actual Android files. The full user grammar, physical-device performance, source browser round trip, offline sync and long-term usefulness remain unproved. Trace rows are retained references; opening kept artifacts is future mobile work.
