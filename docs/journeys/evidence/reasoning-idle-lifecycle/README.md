# Idle direct lifecycle — issue #75

[Release checks](release.json) at clean `e6a5fd6` passed typecheck and **354 backend tests** (12 baseline + 342 additional). The new populated 0010→0011 test preserves all preexisting rows, including sealed Keep/Ask context and a safely withdrawn graph, plus the ten released migration checksums. Owner data was not migrated or cleared.

Original-session authenticated internal cancellation and trusted actual-Job-deadline expiry close bound queued/recovered direct Jobs without a healthy lease. Real SQL checks cover session substitution/revocation, resource-wait expiry, scope/epoch refusal, healthy claim, recovery/unknown accounting and late original settlement, Clear serialization, rollback, write-free replay and seven-day private retirement without recreating Ask execution state. Raw SQL tests preserve the prior fenced live-worker branch and reject clock/fence/binding bypasses.

Fairness tests prove arbitrary non-head removal preserves siblings' cursors/open turns, proven unsent refunds precede idle clamps, and debt/unknown liabilities are not reset by age. Scheduler tests distinguish earlier request expiry from actual Job expiry, reach the latter through maintenance after membership is gone, bypass an oversized head while other work progresses, and roll back injected SQL failure. A single-probe expiry with a real fair reservation commits exactly two scheduler changes (refund plus finalizer) and no stale final class yield. Graph fixtures are synthetic; no provider was invoked.

[Separate maintenance-process proof](retirement.json), at clean `a12d071`, observes an expired bound queued Job with a revoked original session become safely clocked/fenced, keeps its private context for the existing retention horizon, and observes the required expiry aggregate. It also proves aged private retirement, retained unknown accounting, late usage settlement, closed-accounting purge and graceful worker shutdown/disconnect with normal database removal. Fixture-only timestamp aging does not establish seven days of wall-clock operation or deploy the owner process.

Joined regressions retain their actual tested revisions: [J001](j001.json), [J002](j002.json), [J003](j003.json), [J004](j004.json), [Ask HTTP restart/replay/Clear](ask-http.json), [PostgreSQL fairness restart](sql-restart.json), and [eight interruption-cleanup cases](interruption-cleanup.json). J004's verifier passed all thirteen scenarios and four local fixture requests. All 70 recorded source hashes were compared with the final source. J001/J002 report a dirty checkout because operations documentation was being written; runtime source was unchanged. Later commits add tests/docs only. Original #57's missing-acknowledgement cause remains unknown.

The first expanded run passed 353/354 checks but exposed a new fixture assertion that counted withdrawal triggers in both public and isolated schemas. Its [failure excerpt](migration-test-failure.txt) is retained with presentation whitespace trimmed; the original log hash is in the release receipt. Scoping the assertion to the isolated `reasoning_job` relation fixed that test. The test's temporary directory also follows the environment so local storage stays on the SSD and Linux CI uses its configured root. The corrected full suite passed; this was a changed test run, not an unchanged retry.

Sol (`gpt-5.6-sol`) built closure/maintenance and independent scheduler-refund checks. Two Terra (`gpt-5.6-terra`) workers supplied independent privacy/concurrency and SQL/fairness/migration tests and review. The coordinator owns ADR-0018, migration0011, shared contracts, arbitrary membership finalization, scheduler integration and joined receipts. Independent source review accepted `e6a5fd6`; backend and Android CI at the PR head remain the merge gate.

These are internal development primitives, with no new Job, public cancellation/Ask control, answer, live provider call, world evolution or generated Reel. Fresh execution grants, completed/failed private retention, provider budgets and owner UX acceptance remain separate gates. The owner API/projection/maintenance processes were left running, and the dated owner observation still has migrations only through0009. Full mobile/desktop UI, live Cutroom integration and journeys A–I remain required by #72.
