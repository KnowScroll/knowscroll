# Scroll reader and deliberate discovery — issue #74

Verified at clean `6766939dff7daf0e2e9623478e932bb5ea2e7570`, based on merged #66 (`0ada716`). [Reader receipt](reader/release.json), [existing Android receipt](android/environment.json) and [Clear History receipt](history/environment.json) retain their actual revision and source hashes. Coordinator checked every recorded source hash against the integrated tree and collected exit0 from all three scripts. Build, lint, five JVM unit tests and fifteen instrumentation phases passed: four reader phases, five existing Android phases and six Clear History phases.

The reader uses the existing Cosmos palette with compact Home/Keep/Sources controls, a source/truth sheet and explicit discovery at the content's end. At840×1680 with font scale1.3, Sources remains a whole label and controls satisfy48dp targets. [Reading](reader/reader-navigation.png), [sources](reader/reader-source.png), [finite rest](reader/reader-rest.png), [retry](reader/reader-retry.png) and [authority rejection](reader/reader-authority.png) are real emulator captures.

The source flow preserves reading offset804 and the complete retry envelope across Back, Activity recreation with the reader or sheet open, and observed Chrome launch/return. The sheet closes during recreation; the same page/position survives. Chrome's first-run screen was observed, so external source-page loading is explicitly unproved. Three explicit discovery exposures/ledger facts and no Keep Jobs/Traces were recorded; there is no prefetch exposure or invented interest.

The loopback test proxy closes exactly two feed sockets to exhaust the API client's normal retry. The current page, offset and retry identities remain during loading/failure; a deliberate retry advances once, creating exactly two exposures across that scenario (initial and next), despite a duplicate tap. Successful responses come from the real API. A real SQL session revocation then produces one actual feed401; the stale reader/cache/visited state is removed rather than retained under invalid authority. Foreground Clear also removes an open source sheet and private cache.

Existing regressions include cold force-stop/relaunch with the same retry keys and position, duplicate-safe Keep, normal/compact return, API-unavailable recovery, harmless Clear cancellation, real nonempty Clear, dropped committed Clear response across process death, foreground epoch change and changed-universe local purge. Databases were disposable; only the separate `.journey` app was cleared/installed. Owner app/history/services were untouched. No providers were called.

Independent Terra review found the original Activity-recreation position bug. Sol fixed active-state synchronization and the coordinator verified it on the emulator. Test-only corrections handled startup readiness, Back dispatcher compilation, modal visibility and the real external-window transition; no unchanged failing run was rerun to seek green. Original local diagnostic logs remain in the SSD temporary directory. Sol (`gpt-5.6-sol`) implemented the UI; Terra (`gpt-5.6-terra`) independently reviewed it and supplied the privacy/authority tests. Final review and CI are recorded in the PR.

Manual TalkBack traversal, physical-device validation, owner visual/usefulness acceptance, desktop, rich interactive Scroll blocks, branches, Reel/Cutroom, semantic/living/social worlds and full journeys A–I remain unproved. This advances #3/#72 without completing them. The internal Ask compiler remains unrelated to paid-task authorization or a mobile Ask control.
