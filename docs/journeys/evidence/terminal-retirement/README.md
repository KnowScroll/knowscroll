# Completed/failed private retirement — #81

The owner selected seven days after completion or failure for private prompts, frozen context and unsaved outputs. [ADR-0019](../../../decisions/0019-completed-failed-private-retirement.md) and migration0012 add a separate immutable database finish clock and extend the existing bounded maintenance sweep to safely finished private graphs at168 elapsed hours. Saved source history and minimal accounting retain separate policies; Clear History remains immediate.

[Release checks](release.json) passed typecheck, **444 backend tests** (12 baseline plus432 additional), and **44 focused terminal tests** at `bd47c05`. Final runtime source was committed; guidance/evidence edits made the checkout dirty. Populated0011→0012 tests compare every old row/column and all11 released migration checksums. Existing terminal rows get no guessed timestamp. No owner data was migrated or cleared.

SQL tests prove both terminal outcomes and waiting transitions, caller-backdate replacement, immutable clock/state/fence, insertion refusal, safe leases/steps/Attempts/accounting, mixed-clock constraints, migration preservation, retirement boundaries, legacy-null exclusion, rollback, immediate Clear/replay, unchanged unknown liabilities and late settlement. Actual PostgreSQL waits prove that a child made unsafe during a wait is not erased, and a lease expiring while finish-safety inspection is blocked cannot establish the finish clock. Test-only corruptions and clock aging are confined to disposable schemas.

The [separate maintenance-process receipt](retirement.json), at clean `34b2894`, observes completed and failed private graphs disappear at the fixture-aged horizon while young finished context and source history survive. It preserves unknown accounting, permits and reservations, permits authentic late accounting without private resurrection, and retains the separate30-day accounting policy. Graceful worker exit, stopped acknowledgement, pool disconnection and ordinary database removal all passed. The same production hashes remain current; this is not a seven-day wall-clock soak or a deployed completion consumer.

The [J004 regression](j004.json), at `bd47c05`, passed all13 separate-process fault cases and its verifier with four local fixture requests. Both receipts contain14 source hashes, all28 compared against final files. The [failed-run record](failed-runs.json) preserves the earlier deadline-case `reserve / invalid_deadline` denial and successful cleanup. Issue#82 corrects a test-actor request calculation that omitted the Job deadline; the deadline case now deliberately requests the full Job horizon and relies on that cap. No production admission changed. The failed run did not identify which internal deadline predicate fired, and does not establish the cause of historical#57.

MiniMax M3 provided independent design and production/delta reviews, plus a separate clock/migration test lane. [Review disposition](independent-review.json) records exact revisions, usage and corrections. The coordinator rejected inaccurate static claims and false-positive filtered test results, stopped repeated worker suite runs, inspected the authored tests, repaired invalid fixture setup and ran all accepted checks with complete logs and explicit exits. Failed fixtures and corrections remain recorded; reviewer assertions alone are not runtime proof.

No ordinary provider call, answer, saved-output publication, new user control or owner deployment is introduced. A future trusted completion/application consumer must separately validate and apply results before safe terminalization. Future private output storage must join the erasure graph before product use. Full mobile and desktop UI, real Cutroom and journeys A–I remain release requirements under#72; it stays open.
