# Sealed literal Ask context — issue #66

[Release checks](release.json) at clean `8898839` passed typecheck, **323 backend tests** (12 baseline + 311 additional), and diff validation. All nine released migration files remain unchanged. The populated 0009→0010 test compares every preexisting table's rows and prior migration checksums; the owner database was not migrated or cleared.

The compiler consumes a separately authorized caller-supplied queued direct Job, seals literal Unicode question/source bytes and exactly eight typed reads, and creates no execution, accounting or projection rows. Tests prove original-session authority, foreign/old-epoch refusal, asset and ambiguous candidate drift (including mixed-case UUID duplication), immutable binding/family isolation, rollback, malformed/corrupt seals and missing/extra/changed stored reads. Direct Keep V1 is unchanged and works through the explicit family router.

Real competing PostgreSQL clients prove first-family publication serialization; fair admission and dispatch wait on the original session before the Job; an expired session after a physical bucket wait prevents admission without creating an Attempt/Permit/accounting. Recheck acquires no dependency locks. Privacy tests authenticate real local sessions, observe compiler asset contention and database-clock expiry while Clear waits, erase the graph on Clear, and retire a safely withdrawn test graph after an explicitly aged seven-day clock. Exact Ask replay then returns its original `recorded_only` source receipt without recreating execution state.

Joined regressions at clean `bcd621b` retain their original tested revision: [J001](j001.json), [J002](j002.json), [J003](j003.json), [J004](j004.json), [Ask HTTP restart/replay/clear](ask-http.json), [scheduled retirement](retirement.json), and [eight interruption cases](interruption-cleanup.json). J004's verifier passed all thirteen scenarios with four local fixture requests. Only test files changed between that journey revision and the release-check revision; production source is identical. One new adversarial test initially failed because it attempted a guarded immutable write; its corrected form separately checks normal rejection and explicitly injected disposable corruption.

Sol (`gpt-5.6-sol`) implemented the compiler. Separate Terra (`gpt-5.6-terra`) workers supplied contract/migration/concurrency review and privacy/retirement tests. Coordinator inspected actual diffs and integrated the routing/session-lock changes. Independent Terra review accepted `8898839` with no blocking privacy/concurrency findings; the reviewer observed both passing test batches and the coordinator separately collected the final exit-zero receipt. Exact-head backend and Android CI remain the merge gate.

These are internal development operations and synthetic fixtures. There were no live provider calls, answers, generated Reels, mobile controls, production identity or usefulness checks. Historical #57's original missing-acknowledgement cause remains unknown. Owner processes and schema availability are separate operational facts; the running owner API was not reloaded. Full mobile/desktop UX, live Cutroom integration and journeys A–I remain required by #72.

PR CI at `f732578` exposed a new test-observation race: the losing family insert was correctly rejected by PostgreSQL, but its rejection handler was attached only after releasing the blocker. [Failure excerpt](ci-failure-excerpt.txt) preserves the original event; [repair receipt](ci-test-repair.json) records early assertion attachment and settlement before connection cleanup. Production source is unchanged. Typecheck and all 323 backend tests passed at `ea4a5be`; this is a changed test run, not an unchanged retry. The original #57 cause remains unknown.
