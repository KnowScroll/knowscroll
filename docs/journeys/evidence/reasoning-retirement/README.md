# Seven-day withdrawn reasoning retirement — issue #62

[Execution receipt](execution.json) records typecheck and 253 passing backend tests at `a319ed7`, unchanged released migrations 0001–0007, exact source hashes and independent Terra/Sol work. [Scheduled maintenance](scheduled-retirement.json), [six interruption checks](interruption-cleanup.json) and [PostgreSQL restart regression](postgres-restart.json) retain clean source revision `497ff66`; every recorded runtime hash matches the final tested implementation. Subsequent changes added tests and documentation only. J001–J004, two verifier rejection cases and twelve fairness traces also passed.

Real disposable PostgreSQL cases prove the database withdrawal clock, live-lease requirement, immutable stamp, early retention, same-job races, rollback, skipped locked universes, graph limits and unsafe-state exclusions. An in-flight SQL barrier proves abort finishes the current atomic transaction and starts no next probe. Privacy tests prove immediate authenticated clear, exact old-clear replay preserving later work, unchanged unknown reservations during retirement, late original usage without private resurrection, and receipt-held locks preventing accounting purge. Accounting retains its separate all-duties-closed 30-day clock.

The separate-process runner compiles synthetic Scroll contexts through the actual compiler, admits/withdraws Attempts, schedules two batches, removes only due private graphs, preserves young graphs, purges closed accounting, settles/replays late usage and exits cleanly on idle SIGTERM. It removes its disposable database. Seven-day aging is a fixture operation, not seven days of wall-clock observation; active OS-signal timing is not claimed from the SQL abort test.

No provider calls were made. Completed/failed Jobs, legacy terminal rows without an authoritative timestamp and recovered waiting Jobs without a live lease remain retained. These receipts do not establish owner deployment, production capacity, semantic usefulness or complete privacy lifecycle. Owner history and existing API/worker/emulator were preserved. Android build/lint/unit CI is a release gate; instrumentation was not rerun for this backend-only change. Issue #57 remains open because the historical Linux failure's exact cause is unresolved.

After PR #63 merged as `48ab5b2`, the [owner migration](owner-retirement-migration.json) preserved all rows across 32 existing tables and seven prior checksums. The [dated local observation](owner-retirement-deployment.json) records a separately launched default maintenance process, three batches with no owner deletion, healthy API and the unchanged projection worker. This is not an OS-service or auto-restart guarantee. Both Linux/Android CI runs passed at PR head `1c4dce4`: [push](https://github.com/KnowScroll/knowscroll/actions/runs/35109758306), [PR](https://github.com/KnowScroll/knowscroll/actions/runs/35109951781).
