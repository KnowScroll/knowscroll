# Frozen direct Scroll context — issue #60

[Execution receipt](execution.json) records 230 passing backend tests and typecheck at `1901097`, unchanged migrations 0001–0006, exact changed source hashes and independent Sol/Terra review. J001–J004, two verifier rejection cases, six interruption cleanup cases, twelve deterministic fairness traces and the disposable PostgreSQL restart regression passed. [J004](j004.json), [cleanup](interruption-cleanup.json) and [restart](postgres-restart.json) retain their original clean revision `8fc8219`; subsequent changes added tests/documentation only.

Real disposable PostgreSQL tests verify same-client authentication, exact literal source/body freezing, canonical ordering, typed dependency completeness, same-revision drift, historical lineage changes, unrelated Keep independence, scope-bound policy identity, seal/generic append races, lock/recheck phases, session expiry after waits, atomic rollback and clear erasure. Concrete authority is exercised through fair reserve and dispatch authorization. A second session reusing the same Job was an independently discovered counterexample; immutable first-session binding now rejects it and permits a fresh Job/direct intent.

No external provider calls were made. These checks establish an internal development boundary, not proposal validation/application, semantic usefulness, production identity or source truth. The older process fault/restart harnesses still use synthetic authorities; they do not prove a concrete context compiler process-restart journey. Owner history, running emulator and normal API/worker were preserved. Android compile/lint/unit CI is a PR release condition; Android instrumentation was not rerun for this backend-only change.
