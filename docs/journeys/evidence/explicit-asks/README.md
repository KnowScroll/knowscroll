# Literal explicit Ask facts — issue #64

[Release receipt](release.json) records typecheck, 274 passing backend tests and all listed regression runs at clean `aec78d1`, unchanged released migrations0001–0008, and independent Terra/Sol review. Every recorded runtime source hash matches this tested release. Both SQL and API reject ambiguous same-asset candidate identities before validating the single Scroll shape. [Earlier prerelease evidence](execution.json) and its original receipts remain preserved at their original revisions; they do not prove the later ambiguity fix.

[Separate HTTP proof](http-restart-clear-release.json) records/replays a synthetic literal question, rejects changed payload, restarts the actual API and replays the same durable receipt, clears history, rejects an old-epoch Ask retry, preserves later work under old-clear replay, and removes the disposable database/process. No provider or projection worker is used. This proves a recorded fact, not an answer.

Real PostgreSQL/API tests cover exact Unicode bytes and escaped JSON bounds, session-scoped duplicate identity, changed source/candidate/payload rejection, deferred pair consistency, immutable source history, clear-only erasure, forced rollback, foreign isolation and both clear/admission commit orders. Race tests observe the actual blocked backend PID while an authenticated transaction holds the universe lock. Session expiry/revocation after a universe wait and expiry after an asset share-lock wait return401 without writing. Ask cannot be used as Keep evidence by contextV1; no Job, accounting, Accounts or Trace mutation follows recording.

J001–J004, two verifier rejection cases, [six cleanup cases](interruption-cleanup-release.json), twelve fairness traces, [scheduled retirement](retirement-release.json) and [disposable PostgreSQL restart](postgres-restart-release.json) regressions passed. The PostgreSQL restart receipt remains an existing synthetic reasoning test; the Ask-specific receipt restarts API only. Android build/lint/unit CI is required before merge; owner instrumentation was not rerun.

Source Ask history remains private until Clear History. It is separate from seven-day withdrawn execution-context retention. No mobile Ask control, reasoning queue, answer, inferred learning, semantic application, provider call, production identity/capacity or usefulness is claimed. The normal owner API is not implicitly restarted by pulling source.
