# Saved Trace source revisit — issue #78

A real saved Trace now opens its original selected Scroll after current-scope, causal-lineage and source validation. Reopening is read-only. Exact source drift produces an unavailable state; it never silently substitutes newer content. The [read contract](../../../contracts/trace-revisit.md) defines the narrower source-history boundary.

## Evidence

[Integration receipt](release.json) records 400 backend tests (12 baseline +388 additional), typecheck, nine Android JVM tests, build/lint and23 instrumentation phases. Backend ran at clean `cfa27a7`; the final eight-phase saved-Trace journey ran at clean `45304d2`. Fifteen existing device regressions and J001–J003 ran at `7132c84`. Later edits changed the saved-Trace test/harness and documentation only; production source hashes remain identical. The coordinator verified92 source hashes against the current tree or the receipt's exact historical commit before adding documentation.

[Saved-Trace receipt](trace/release.json) joins actual API, separate projection worker, PostgreSQL and API36 emulator. At840×1680/font1.3 it performs a UI Keep, waits for projection, opens the Trace, asserts exact selected title/body/source/revision, recreates the Activity with the same origin/position, then force-stops and relaunches with an observed PID change and fresh authorized Trace read. It checks sources and return, exactly two dropped read sockets with same-identity retry, changed-source409 with local removal, explicit Next, Clear, and actual revoked-session401. The final authority phase uses native AVD size/font1.0. Successful content always comes from the real services; faults intentionally alter only disposable state.

Full hashes of every private table except operational heartbeat/migration rows remain unchanged through read-only phases. Initial Keep produces one decision/exposure/Job/Trace and two Ledger facts. Explicit Next adds exactly one decision, one exposure and one Ledger fact, with no Keep or reasoning work. Clear removes the counted private graph. Revocation is observed at `/v1/universe` before an additional Trace read; the app does not make a needless unauthorized read. All service groups exited, the disposable database was dropped and display/font settings restored. No product providers were called.

The existing [reader](reader/release.json), [Android](android/environment.json) and [history](history/environment.json) receipts preserve their actual source revisions. They cover sources/navigation/retry/rest/authority, Keep and cold restoration, and ambiguous Clear/recovery/privacy scope. [J001](j001.json), [J002](j002.json) and [J003](j003.json) retain separate HTTP/process lineage and isolation evidence. Actual saved-Trace captures are retained in `trace/`; the coordinator inspected reading, sources, unavailable-source and authority states. Physical-device performance, manual TalkBack and owner visual acceptance remain unproved.

## Review and corrections

Sol (`gpt-5.6-sol`) implemented SQL/API and reviewed mobile; Terra (`gpt-5.6-terra`) implemented mobile and independently reviewed contract/API/privacy. Their earlier review corrected persisted-origin removal after authority rejection and malformed-response handling. The resumed coordinator uses MiniMax M3 through pio for an additional independent read-only review under the owner's updated delegation instructions; [its final static review](independent-review.json) found no privacy blockers at `cfa27a7`, with no test execution claim. The coordinator inspected actual code and receipts; later code changes only strengthen device verification.

[Failed attempts](failed-runs.json) preserve raw SSD log hashes and diagnoses: missing UI Keep preparation, a startup-navigation readiness race and a test-only invalid Compose import. Every retry followed a concrete correction. Checked-in instrumentation text trims only surrounding blank lines; original SSD artifacts/log hashes retain raw output. One failed attempt also reported a cleanup PermissionError; its exact throwing operation was not recorded, so no exact cause is claimed. The successor harness inspects actual process-group membership and retains detailed future cleanup errors. This does not resolve historical #57.

## Limits and continuation

This is an intermediate #3/#4/#12/#72 capability. Public identity, historical source-version/correction UX, richer Scrolls, Ask answers, desktop, semantic/living/social worlds and live Cutroom/Reels remain incomplete. Only identity/scope/revision/position are newly persisted for revisits; there is no offline body cache. Clear and source/session rejection discard the pending origin. Owner runtime was not upgraded: on September19 its database remained through0009 and its API/projection processes were stopped; the dedicated SSD PostgreSQL cluster was restarted without migrations. All destructive checks used disposable databases and the `.journey` Android app.
