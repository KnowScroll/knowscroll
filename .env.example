# Local development only. Run ./scripts/dev-init.sh to create .env safely.
DATABASE_URL=postgresql://knowscroll:REPLACE_WITH_LOCAL_PASSWORD@127.0.0.1:55432/knowscroll
KS_DEV_TOKEN=REPLACE_WITH_RANDOM_LOCAL_TOKEN
PORT=4310
# Optional local secret storage. The certification CLI reads only its process environment.
# See docs/operations/minimax-certification.md; ordinary jobs do not use this key.
MINIMAX_API_KEY=
CUTROOM_BASE_URL=
